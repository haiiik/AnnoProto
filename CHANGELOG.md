# Changelog

本项目所有重要变更记录在此文件。**每次更新 GitHub 时需同步更新本文件**，在顶部新增条目。

---

## [1.2] - 2026-08-04

### 功能增强
- 抽屉功能按钮区移至**底部固定按钮栏**（添加标注 / 保存 / 标注点显隐 / 收起），头部仅保留页面名与标注数。
- 新增**手动添加标注**：抽屉底部"添加标注"按钮 → 拾取模式（元素 hover 高亮）→ 点击元素 → 表单填写主标题/类型/详情/区块 → 写回 `data-annotate` 属性，可随保存导出；区块留空自动归组。
- 新增**删除标注**：卡片右上角 ×（hover 显示）→ 确认 → 移除 `data-annotate` 相关属性、圆点与卡片，序号自动重排。
- 新增**元件内容同步**：双击编辑被标注元素文字后，若主标题与元素原文字相同则同步跟随新文字（同名才跟随，业务命名不覆盖）。
- SKILL.md 新增"常见问题（FAQ）"章节（10 问）。
- 文档同步：标注标准交互行为表（添加/删除标注行）、3.1 抽屉结构、SKILL 阶段说明与摘要。

## [1.1] - 2026-08-03

### 本次发布（GitHub Releases 1.1）
- 项目定名 **AnnoProto**（中文名"编注成型"）；仓库名先后演进：annotated-prototype-builder → annotated-prototype-builder-skill → AnnoProto。
- 技能注册名改为"编注成型"。
- README 新增"效果示例"链接（workbuddy.link）与"版本更新记录"章节；使用方法改为通用 skill 描述（不特指某一平台）。
- 仓库新增示例文件 `examples/order-management-prototype.html`（GitHub Pages 已启用）。

### 结构化格式调整
- 必填字段取值改为 `必填：是` / `必填：否`（是=必填，否=选填；原为"必填：必填/选填"）。
- 结构化格式中所有字段统一加"字段名："前缀（`类型：`、`规则/交互逻辑：`、`必填：`、`默认值：`、`数据来源：`、`说明：`），便于识别各字段含义。
- 去掉结构化格式中的"字段名称"（由主标题 `data-annotate` 承载，格式内不重复）。
- 默认值字段带"默认值："字样（如 `默认值：空`）。

### 业务逻辑标注优化（10 项）
- 规则表达模式统一三段式：`当[条件]时 → [行为]；否则[兜底]`。
- 新增业务逻辑前缀段落：`前置条件：` / `异常：` / `权限：` / `流转：`（状态流转路径）。
- 输入校验用业务语言，禁止写正则/技术格式。
- 禁止技术状态值，用业务口径（如"订单已支付"而非"status=1"）。
- 列表数据列补充数据来源业务语义（来源系统、同步频率、更新时机）。
- 跨元素业务关联（如填地址自动计算运费）写入"全局"分组标注。
- 业务关键元素详标业务逻辑，装饰/占位元素简标。

### 容量与展示
- 详情长度上限：100 字 → **300 字**。
- 抽屉宽度：320px → **480px**（移动端 88%，最大 320px 不变）。
- 校验脚本 `DETAIL_MAX = 300`。

---

## 2026-08-02

### v2.0.0 重构：气泡式 → 右侧抽屉式
- 标注展示改为页面右侧固定抽屉浮窗，按页面区块分组展示，全页连续编号。
- 抽屉右上角仅"保存"和"收起"按钮；收起后页面右下角显示"展开标注"按钮。
- "保存"按钮语义：回写标注文本到 data 属性 + 下载单文件 HTML（文件名加 `-annotated` 后缀）。
- 页面元素保留序号圆点（全页连续编号，点击可在抽屉中定位）。

### 新功能
- **全局分组**：`data-annotate-section="全局"` 的标注归入"全局"分组，排所有分组最后（渲染顺序：普通区块 → 其他 → 全局）。
- **序号重排**：抽屉内序号按显示顺序编排，保证永远从小到大；页面圆点序号与卡片一致。
- **标注点显隐**：抽屉"隐藏圆点/显示圆点"按钮，切换页面元素序号圆点显隐。
- **页面文字编辑**：双击页面任意文字（标题、正文、按钮、表格内容等）就地编辑，导出时保留修改。
- **圆点联动**：点击页面序号圆点，抽屉自动展开（若收起）并滚动定位到对应卡片。

### 挂载约定
- 新增 `data-annotate-section`（区块容器）、`data-page` + `data-page-title`（多页面）。
- 页面切换由原型自带导航触发（`window.__annoGotoPage` 或 MutationObserver 监听页面可见性）。

### 标注规则
- 带固定规则的标签文本、列表数据列（无交互）纳入标注范围（列表数据列须标明数据来源）。
- 标注来源优先级：上游需求文档 > 技能兜底标准（含需求文档判断机制：路径探测 → 消息识别 → 上游传入 → 主动询问，需用户确认采用）。
- 详情长度上限：60 → 100 字（后于 2026-08-03 调至 300 字）。
- 结构化格式：表单字段 `类型|规则/交互逻辑|必填|默认值`、列表数据列 `类型|数据来源|说明`（后演进为带前缀格式）。

### 其他
- 去除原型视觉设计规范（页面底色、排版、线框风等），需搭配专门的设计规范 skill；导航栏样式不由本 skill 规定，优先沿用需求文档。
- 技能重命名为"生成可编辑标注的原型"。

---

## v1.0.0（2026-08-02 之前）

### 初始版本
- 气泡式标注引擎：每个可交互元素右侧浮气泡，双击文本就地编辑。
- 顶部居中工具栏（全部展开/折叠、显示/隐藏标注）+ 右下角"导出"按钮。
- 元素识别规则、标注文案规范（主标题 ≤12 字、详情 ≤60 字）、六类类型枚举。
